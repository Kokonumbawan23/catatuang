{
  "$schema": "https://railway.app/schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "rm -rf vendor/composer && composer install --no-dev --no-progress && composer dump-autoload",
    "startCommand": "php artisan serve --port=$PORT"
  },
  "deploy": {
    "numReplicas": 1,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  },
  "database": {
    "default": "sqlite"
  }
}
