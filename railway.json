{
  "$schema": "https://railway.app/schema.json",
  "build": {
    "builder": "railpack",
    "buildCommand": "composer install --no-dev --no-progress && npm ci && npm run build"
  },
  "deploy": {
    "numReplicas": 1,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "startCommand": "php artisan migrate --force && php artisan config:cache && php artisan route:cache && php artisan serve --port=$PORT"
  }
}
